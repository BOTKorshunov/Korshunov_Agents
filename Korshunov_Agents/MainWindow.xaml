<Window x:Class="Korshunov_Agents.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Korshunov_Agents"
        mc:Ignorable="d"
        Title="Попрыженок" Style="{StaticResource windowStyle}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Style="{StaticResource secondColorBackground}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Image Source="Images/Попрыженок.png" Height="64" Margin="10, 0, 10, 0"/>
                <Label Content="Попрыженок" FontSize="25" VerticalAlignment="Center"
                       FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <Frame Grid.Row="1" Name="fMain"/>
    </Grid>
</Window>
